{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Net Pulse: Build engine (Node-API addon)",
      "type": "shell",
      "windows": { "command": "npm install; npx cmake-js rebuild --runtime electron --runtime-version $(node -p \"require('../electron/node_modules/electron/package.json').version\")" },
      "linux":   { "command": "npm install && npx cmake-js rebuild --runtime electron --runtime-version \"$(node -p \"require('../electron/node_modules/electron/package.json').version\")\"" },
      "options": { "cwd": "${workspaceFolder}/napi" },
      "problemMatcher": []
    },
    {
      "label": "Net Pulse: Build UI",
      "type": "shell",
      "command": "npm install && npm run build",
      "options": { "cwd": "${workspaceFolder}/web" },
      "problemMatcher": []
    },
    {
      "label": "Net Pulse: Install Electron",
      "type": "shell",
      "command": "npm install",
      "options": { "cwd": "${workspaceFolder}/electron" },
      "problemMatcher": []
    },
    {
      "label": "Net Pulse: Run app",
      "type": "shell",
      "command": "npm start",
      "options": { "cwd": "${workspaceFolder}/electron" },
      "dependsOn": ["Net Pulse: Install Electron", "Net Pulse: Build UI", "Net Pulse: Build engine (Node-API addon)"],
      "dependsOrder": "sequence",
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": []
    },
    {
      "label": "Net Pulse: Run C++ unit tests only",
      "type": "shell",
      "command": "cmake --build build --config Debug --target netpulse_tests && (cd build/Debug && ./netpulse_tests || .\\netpulse_tests.exe)",
      "options": { "cwd": "${workspaceFolder}" },
      "problemMatcher": []
    }
  ]
}
