export default function ToolUnavailable() {
  return <div className="tool-note">These tools run in the desktop app (they use the OS network stack). Open Net&nbsp;Pulse as the desktop app to use them.</div>
}
